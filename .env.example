# Django
SECRET_KEY=troque-por-uma-chave-segura
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOW_ALL_ORIGINS=true

# PostgreSQL
DB_NAME=arayzap
DB_USER=postgres
DB_PASSWORD=sua_senha
DB_HOST=localhost
DB_PORT=5432

# Criptografia de credenciais (opcional; sem isso armazena JSON em texto)
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# Meta / WhatsApp Cloud (webhook)
META_APP_ID=
META_APP_SECRET=
META_VERIFY_TOKEN=

# Bootstrap do primeiro token (desligar em produção)
ALLOW_TOKEN_BOOTSTRAP=true
BOOTSTRAP_SECRET=defina_um_segredo_longo

# PyWaBot — a lib usa servidor embutido; URL extra reservada para forks
PYWABOT_DEFAULT_BASE_URL=
